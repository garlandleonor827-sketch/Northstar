# 本次交接包检查报告（初始基线）

> 这是交接包在实现前的基线报告。Windows 原生开发启动的后续实测请以 [ACCEPTANCE_REPORT.md](ACCEPTANCE_REPORT.md) 为准；本文件保留原始素材与规则检查范围。
日期：2026-09-20。交付对象：合同、参考素材、规则样例；不是完成后的Windows应用。

## 已执行并通过
| 检查 | 结果 | 证据 |
|---|---|---|
| 确定性规则参考测试 | **45 / 45 PASS** | rules.tap；node --test tests/contract.test.mjs |
| 素材/结构/渲染检查 | **48项检查 PASS**（其中包含一次规则测试汇总，不与45项相加） | checks.json |
| JSON Schema | 两份Schema有效；对应样例通过；额外字段被拒绝 | checks.json |
| PNG/WebP | 全部可解码，Core透明通道与四角透明通过；UI参考图1600×1000 | checks.json |
| 配色对比 | 12组语义前景/背景均≥4.5:1；最低5.08:1 | checks.json；这是token对比检查，不是完整无障碍审计 |
| 素材浏览页 | Chromium渲染6张图片成功，无脚本错误；1600/1280/1024/768宽度无页面横向溢出 | gallery-browser.png |
| 文件与打包 | SHA-256、文件清单、Markdown本地链接、SVG XML、字体/疑似Key扫描、ZIP CRC与解压复核通过 | MANIFEST.sha256；tests/verify_bundle.py |
| 视觉检查 | 深/浅首页、复盘页、成长方向图均已查看并修正残留按钮/文字、边缘和主题适配问题 | assets/ui/ |

## 规则测试覆盖的重点
越权操作、硬截止/锁定事实、未知ID、版本冲突、整批回滚、幂等/重复ID、原型键名、依赖、容量、撤销冲突、缺失数据、经验去重/封顶/更正、API根路径与HTTPS规则。
这些测试执行的是reference/rules.mjs，不是尚未编写的Rust领域层。Codex必须移植规则并重新测试。

## 未验证 / 明确限制
- **Windows应用运行：未验证。** 本次环境为Linux，没有完整Tauri应用；无安装包。
- **真实API联调：未验证。** 未使用任何真实密钥或付费调用；接口路径规则不等于供应商联调成功。
- **实时3D、帧率、Windows凭据库、实际SQLite事务/迁移/备份恢复、应用E2E：未验证。** 这些属于Codex实现验收。
- **浏览页直接file://打开：受本环境浏览器策略限制，未验证。** 已用相同HTML及内联素材完成渲染测试，没有修改浏览器安全策略；文件间链接另用本地路径检查。
- **Core静态素材分辨率：已知限制。** 原始核心裁切311×214，置入1024×1024透明画布，插值未增加细节；正常模式必须实时渲染，不用PNG旋转冒充3D。
- 原始生成拼贴有不准确日期/文案，只作为灵感；最终界面以assets/ui/与合同为准。

## 本轮修正
1. 从Core裁切中移除原界面按钮残影，抑制周边背景，补充浅色版本和透明度检查。
2. 示例页面改用有效的2026日期与周期标签；Windows快捷键写作Ctrl K。
3. 明确AI不能以减少“预计时长”偷偷改变任务定义；仅软排程可自动调，其余需提案。
4. 幂等收据用Map避免特殊键名问题；撤销增加当前容量检查，不能恢复出超负荷计划。
5. 把源图、降级素材、界面参考和实时3D要求分开，避免Codex误把截图当应用。

## 环境与复核
Python 3.13.5 / Node v22.16.0 / Chromium 144.0.7559.96。
仅完整性检查需Python标准库；规则检查仅需Node。

```sh
python tests/verify_bundle.py
node --test tests/contract.test.mjs
```
本报告只对上述实测范围负责；不能据此宣称完整桌面软件已经通过验收。
