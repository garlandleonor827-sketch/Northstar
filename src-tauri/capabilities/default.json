{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Northstar desktop window with no shell, filesystem, or external network capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
