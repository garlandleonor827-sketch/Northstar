# 应用验收（交给 Codex 执行，不代表现已通过）
状态只允许 PASS / FAIL / BLOCKED，并记录命令、环境、日志或截图。

| ID | 必须通过的场景 |
|---|---|
| A01 | 无Key初始化 → 确认季度主线 → 手动安排今日 → 完成/卡住 → 周复盘，重启后保留 |
| A02 | 10h降为4h，软排程总分钟≤240；不删任务定义、不碰锁定职责/硬截止/历史事实 |
| A03 | 恢复模式暂停可选行动；容量不足容纳锁定事项时显示冲突，不静默移动 |
| A04 | 同季度新主线必须替换审批；维护任务、责任与停车场想法不误判第二主线 |
| A05 | 模型试图修改年度目标/财务/过去耗时/权限被后端拒绝；客户端UI不能绕过 |
| A06 | 同请求重复不重复执行；同ID不同内容拒绝；旧版本提案/自动调整不覆盖新数据 |
| A07 | 一批含非法操作整批回滚；崩溃注入不产生半更新；撤销有版本冲突提示 |
| A08 | 新想法冷却14天；可编辑/延期/放弃；提前升级明确确认，不能无声叠加主线 |
| A09 | 没记录显示未知；恢复周不被算作零完成惩罚；任何“下降X%”可追溯原始数值 |
| A10 | 记忆需确认；显示来源/推断；删除后检索和上下文排除，提醒旧备份残留 |
| A11 | 同成果重复勾选/重复复盘/恢复切换不会刷经验；休息不扣Stage；更正证据不降最高Stage |
| A12 | 两个mock Provider：JSON schema+stream 和 basic text；可手填模型；参数降级有提示 |
| A13 | API 401、429、5xx、超时、取消、SSE截断、非法JSON、拒绝都不误写，不泄露Key |
| A14 | /v1与自定义api_root路径正确；网络重定向不跨主机泄露Key；新服务商需上下文授权 |
| A15 | Key不在SQLite/日志/导出/前端持久缓存；窗口最小权限；敏感上下文默认不发送 |
| A16 | 导出→空库恢复一致；不含凭据；损坏/旧版本/不支持版本输入拒绝或可控迁移；现有库不损坏 |
| A17 | 深浅色完整；1600×1000、1280×800、1024×720可操作；125%/150%DPI、中文长标题无截断关键按钮 |
| A18 | Core实时几何确实变化；正常不是旋转图片；无WebGL/减少动效降级，后台暂停渲染 |
| A19 | 键盘操作、焦点、对比、取消请求、空/加载/失败状态完整；庆典可跳过，无催逼/羞辱文案 |
| A20 | 时区/跨午夜/夏令时、周起始、重启后专注计时、重复结束计时与实际耗时独立正确 |
| A21 | 财务仅手动快照；单位和归属清楚，不自动理财、交易、估算未来收益或把未知当0 |
| A22 | 断网核心功能可用；关闭应用不声称持续监测；服务商失败不无声切至另一服务商 |
| A23 | Windows实际启动＋系统凭据读写＋SQLite迁移＋网络适配验收；非Windows只能写BLOCKED |
| A24 | 无密钥时真实API联调标BLOCKED；有授权后记录至少两个用户选定兼容端点，不把mock算真实通过 |

## 交付清单
源码、lockfile、迁移、运行命令、测试日志、主题截图、KNOWN_LIMITATIONS.md。
本阶段不制作安装包。应用功能测试与本包reference测试必须分开汇报。
