# Northstar / 北辰 · Codex 开发合同
版本 2.0 · 2026-09-20 · 本文件替代旧合同；先开发可运行版本，不制作安装包。

## 1. 要做什么
Windows 优先、本地优先的 **AI 个人战略操作系统**。把长期方向变成今日行动，依据真实反馈调整计划；不是普通待办，也不是聊天框套壳。
**成功闭环：确认战略 → 周计划 → 今日执行 → 复盘 → 有边界的重排 → 记忆与成长。**
默认中文，时间/时区读取系统并可修改；示例数据全部虚构，不把用户真实个人信息写入源码。

## 2. 本轮交付范围
| 必须完成 | 暂不做 |
|---|---|
| 本地档案、战略层级、今日/周计划、专注与反馈 | 安装包、自动更新、代码签名 |
| 周/月/季度复盘，计划变更审批、审计与撤销 | 手机端、云同步、账号、团队 |
| 灵感停车场、可确认/可删的长期记忆 | 日历/邮箱/银行等外部集成 |
| 多个 OpenAI 兼容服务商、角色模型选择 | 多智能体自主循环、网页自动操作 |
| 实时抽象核心、成长系统、深浅主题、静态降级 | 人物/动物模型、排行榜、付费养成 |
| 手动财务快照、导出导入、备份恢复 | 交易、理财推荐、以他人身份经营 |
不因功能多而扩张范围。不接 API 时，手动规划、记录、复盘、导出仍能用。

## 3. 核心页面与操作链路
| 页面 | 必须发生的事 |
|---|---|
| 初始化 | 价值/红线/约束 → 长期方向 → 当前季度主线 → 每周容量 → AI 授权；未知可跳过，先存草稿再确认。API 可稍后设置。 |
| 今日 | 今日一个首要行动＋最多三个关键动作；本周成果、季度主线为次级信息。开始 → 完成/部分完成/卡住/不值得继续 → 下一步。 |
| 战略与计划 | 原则 → 10年 → 3年 → 年 → 季 → 月 → 周 → 日。允许中间层为空，不强迫填满。周视图可手动排程。 |
| 复盘 | 周五问：成果、实际投入、阻碍、新方向、下周变化；展示事实与缺失，再给保留/停止/调整。月看模式，季看方向。 |
| AI 战略会话 | 点核心展开右侧工作区；思考/规划/挑战/复盘为四种提示角色，不是四个常驻代理。显示依据、提案和已执行状态。 |
| 灵感与记忆 | 想法默认冷却14天，期间不变任务；到期可放弃/延长/申请升级。记忆区分陈述/行为/推断，候选记忆需确认。 |
| 设置 | Provider、模型路由、容量、主题、动效、时区、隐私、财务快照、备份。核心成长详情收进侧栏，不占首页。 |
每天结束可一键记录成果/变化/明日下一步；漏记不惩罚、不自动补造数据。

## 4. 不可破坏的权限边界
**模型只输出候选命令；Rust 领域层决定能否落库。提示词不是权限系统。**
| 操作 | 权限 |
|---|---|
| 同一已批准周成果内，移动未锁定任务、改变顺序、延后可选动作 | 用户首次开启自动调整后可自动执行；显示变更摘要及撤销 |
| 改任务定义/验收标准、拆合任务、增删动作、跨周延期、改本周成果 | 生成短期提案，用户确认后执行 |
| 改月度里程碑、季度主线、年度/长期方向、价值、家庭/健康底线 | 生成战略提案，必须用户确认；默认整单批准/驳回 |
| 改已完成记录、真实耗时、硬截止、锁定职责、财务、权限设置 | AI 禁止直接写；必要时仅建议用户操作 |
同季度最多一个 active 主线；英语/健康/家庭/本职属于维护或责任，不计作额外主线。
任何写入都验证目标关系、容量、锁定项、依赖、硬截止、baseVersion、命令白名单；不接受任意 JSON 路径或 SQL。
**事务＝变更＋版本＋审计＋幂等记录；失败整批回滚。** 同 requestId 不重复执行；同 ID 不同内容拒绝。
撤销使用补偿事务且检查当前版本，不能覆盖后续编辑；历史事实不被 AI 回写。用户直接编辑也走领域层。

## 5. 动态规划与防漂移
- 容量初值：保底4h / 标准10h / 宽裕14h / 恢复0h，均可编辑；只指自主成长，不挤占职责、睡眠、家庭。
- 用户报告容量下降/任务卡住/进入恢复模式 → 先本地检验 → 重排或提案；AI 失败则保留原计划并可手动改。
- 恢复模式由用户即时切换；仅暂停可选成长任务，不更改硬截止。恢复时重新规划，不把积压任务全部塞回。
- 连续两次有效周复盘且完成率低于60%，或容量下降25%，提示重评；阈值是可调产品假设，不是科学判定。
- 未记录≠0、任务取消≠完成、部分完成不算全部完成；同时展示已知事实与记录覆盖度，缺数据时显示“证据不足”。
- 漂移只关注实际启动/申请替换的主线，不因停车场多记想法或练英语而报警。不推断心理诊断。
- 冷却期间可编辑或放弃；提前升级必须明确选择替代主线/作为辅助并确认，审计记录理由。
- 本地规则/AI请求仅在应用运行及用户操作或已授权复盘触发时运行；关闭应用不假装持续监测。

## 6. Core：质感、成长、状态
视觉遵循 [DESIGN.md](design/DESIGN.md) 与 [参考图](assets/ASSET_GUIDE.md)。**今日任务第一视觉层；抽象核心第二层。**
默认深色：黑钛、烟熏玻璃、冰蓝/冷白银；浅色另做冷白银灰。商务精致，不做满屏霓虹、玩具模型或默认后台。
Core 必须实时程序化（Three.js/WebGL）：不规则几何、少量轨道、受控光场；可受状态驱动，禁止用转 PNG 冒充3D。
长期 Stage I–VII 只进不退；短期 Focused/Drifting/Overloaded/Recovering/Dormant/Unknown 独立于等级，可解释、可纠正。
成长依据已确认成果/里程碑/复盘/取舍；本地确定性积分、去重、封顶，详见 core-rules.json。任务数量/时长不直接换经验。
不因休息扣分，不靠切换恢复模式刷分；不把遗忘记录解释成退步。不弹强制全屏庆典；升级动效可跳过、可关闭。
API 请求中的等待/输出/失败为独立视觉状态，不伪造“思考过程”、人格理解程度或算法置信度。
无WebGL/减少动效/低性能 → 静态透明素材；任务功能不依赖Core渲染。窗口隐藏暂停动画。

## 7. AI 接口与数据安全
配置多个 Provider：名称、API Root、Key、Model；手动模型ID必须可用，列模型与流式为能力选项，不假设全部兼容。[S1–S2]
V1 采用 Chat Completions 兼容适配器：POST {apiRoot}/chat/completions；保留自定义根路径，不重复追加/v1。
展示最终请求地址；默认HTTPS，仅显式本地模式允许环回HTTP。禁止隐式重定向到其他主机、禁用TLS校验或无声切服务商。
角色可同用一个模型。参数按能力发送；不硬编码模型名、费用、temperature或token字段。JSON Schema支持可探测/手动设定。
请求需支持取消、超时、最多2次有界网络重试；401不重试。结构错误最多1次修复，仍失败则不写。流式未结束、拒绝、截断都不得提交。
上下文只选本次相关已确认战略、当前计划、近期事实；发送前可预览类别及条目。财务/家庭敏感详情默认不发，新增目的地重新授权。
Key只由后端写入Windows凭据库；前端只在首次录入时短暂持有，随后仅凭据引用；不进SQLite、日志或导出。[S3–S4]
数据库默认本地明文，并明确告知；不要声称“本地=加密”。不收集患者、单位保密信息；不提供隐身/绕过管理功能。
聊天记录保留可配置；日志默认仅错误码/时延/目标站点，内容与Key脱敏。删除记忆立即排除检索，提示旧备份需另行处理。

## 8. 工程选择与数据契约
Tauri 2 + React + TypeScript + Vite；Rust领域层/SQLite/HTTP/凭据；Three.js Core；先用系统字体，不打包字体文件。
UI仅调用显式命令；不用前端通用SQL执行器，不给模型文件系统/shell/网络工具。Tauri capabilities 最小化。[S3]
实体与事务边界见 [DATA.md](contracts/DATA.md)；自动调整结构见 [schema](contracts/week-change.schema.json)。
金额用整数最小货币单位＋currency，时间UTC存储＋用户时区显示；周起始可配置；实际耗时与计划时长分表。
迁移可重现。备份采用SQLite一致性快照，不裸拷活动WAL数据库；导入先校验版本/结构并预览，备份旧数据后原子替换，排除凭据。

## 9. 开发顺序（每步可运行，不在末尾补视觉）
| 阶段 | 交付 | 通过条件 |
|---|---|---|
| P0 | Today深浅主题＋Core实时原型＋无Key演示 | 与参考图同方向；最小窗口可用；非海报式不可点击截图 |
| P1 | SQLite、战略/任务/记录、停车场、规则与审计 | 断网完整CRUD；唯一主线与权限测试通过 |
| P2 | Provider、会话、结构校验、周重排、提案 | 模拟异常通过；有Key时再测真实端点 |
| P3 | 复盘、记忆、成长、财务快照、备份恢复 | 端到端闭环＋数据往返一致 |
| P4 | 易用性、响应式、无障碍、性能、Windows验证 | 验收报告逐条列出通过/失败/未验证；不制作安装包 |
阶段内自行推进；仅破坏性数据迁移、扩大范围或不可逆外部动作需要再次确认。

## 10. 完成标准
按 [ACCEPTANCE.md](ACCEPTANCE.md) 执行：lint/typecheck/unit/integration/E2E、深浅主题截图、错误/空/加载状态、键盘、125%/150%DPI。
提供运行命令、依赖锁文件、数据库迁移、测试日志、已知限制。没有测试环境/密钥的项目必须标“未验证”，不能报全通过。
**本包的参考规则测试仅验证合同语义，不替代Rust后端、实时Core、真实API或Windows应用验收。**
来源与兼容性说明见 [SOURCES.md](SOURCES.md)；参考图是外观标尺，不能当完成功能或可交互原型。
